query episodes($page: Int, $filter: FilterEpisode) {
  episodes(page: $page, filter: $filter) {
    info {
      ...Info
    }
    results {
      id
      name
      air_date
      episode
      created
    }
  }
}
